import {Dimensions} from 'react-native';
export const windowWidth = Dimensions.get('window').width;
export const windowHeight = Dimensions.get('window').height;

export const gapSize = Dimensions.get('window').width / 22;
export const iconSize = Dimensions.get('window').width / 15;
export const miniPlayerHeight = Dimensions.get('window').height / 15;
